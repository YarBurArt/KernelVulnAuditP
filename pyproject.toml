[project]
name = "kernelvulnauditp"
version = "0.1.0"
description = "This tool checks the practical functionality of linux kernel exploits"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flet",
    "httpx>=0.28.1",
    "sqlalchemy>=2.0.48",
    "streamlit>=1.55.0",
]

